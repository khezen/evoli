machine:
  environment:
    IMPORT_PATH: "github.com/$CIRCLE_PROJECT_USERNAME/$CIRCLE_PROJECT_REPONAME"
# dependencies:
#   pre:
#     - go get github.com/tools/godep
#   override: #if already vendored, build can fly
#     - mkdir -p "$GOPATH/src/$IMPORT_PATH"
#     - rsync -azC --delete ./ "$GOPATH/src/$IMPORT_PATH/"
test:
  pre:
    - go build
    - go vet ./...
  override:
    - echo "" > coverage.txt
    - for d in $(go list ./... | grep -v vendor); do
         go test -race -coverprofile=profile.out -covermode=atomic $d;
         if [ -f profile.out ]; then         
            cat profile.out >> coverage.txt    
            rm profile.out
        fi
      done
  #  - go test -race -coverprofile=coverage.txt -covermode=atomic -coverpkg "github.com/Khezen/darwin","github.com/khezen/darwin/population","github.com/khezen/darwin/population/individual","github.com/khezen/darwin/population/selecter"
  #   - godep go test ./...
  post:
    - bash <(curl -s https://codecov.io/bash)
