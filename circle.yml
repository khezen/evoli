machine:
  environment:
    IMPORT_PATH: "github.com/$CIRCLE_PROJECT_USERNAME/$CIRCLE_PROJECT_REPONAME"
# dependencies:
#   pre:
#     - go get github.com/tools/godep
#   override: #if already vendored, build can fly
#     - mkdir -p "$GOPATH/src/$IMPORT_PATH"
#     - rsync -azC --delete ./ "$GOPATH/src/$IMPORT_PATH/"
test:
  pre:
    - go vet ./...
  override:
    - go test -race -coverprofile=darwin.txt -covermode=atomic .
    - go test -race -coverprofile=population.txt -covermode=atomic ./population
    - go test -race -coverprofile=individual.txt -covermode=atomic ./population/individual
    - go test -race -coverprofile=selecter.txt -covermode=atomic ./population/selecter
  #   - godep go test ./...
  post:
    - bash <(curl -s https://codecov.io/bash)


  